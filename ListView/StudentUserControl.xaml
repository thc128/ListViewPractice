<UserControl x:Class="ListView.StudentUserControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="300" d:DesignWidth="300">
    <Grid>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="20"/>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>
            <CheckBox Name="MyCheckBox" Grid.Column="0"  />
            <TextBlock Name="FirstBlock" Grid.Column="1" Text="{Binding ID}"/>
            <TextBlock Name="SecondBlock" Grid.Column="2" Text="{Binding Name}"/>
            <TextBlock Name="ThirdBlock" Grid.Column="3" Text="{Binding Year}"/>
    </Grid>
</UserControl>
